name: Deploy Alpha Version

on:
  pull_request:
    branches:
      - master

jobs:
  test-secret:
    runs-on: ubuntu-latest
    if: github.event.pull_request.head.repo.full_name == github.repository
    steps:
      - name: Check required tokens
        run: |
          FAILED=false

          if [ -z "${GH_ACTIONS_TOKEN}" ]; then
            echo "❌ GH_ACTIONS_TOKEN is NOT set"
            FAILED=true
          else
            echo "✅ GH_ACTIONS_TOKEN is set"
          fi

          if [ -z "${NPM_PUBLISH_TOKEN}" ]; then
            echo "❌ NPM_PUBLISH_TOKEN is NOT set"
            FAILED=true
          else
            echo "✅ NPM_PUBLISH_TOKEN is set"
          fi

          if [ "$FAILED" = true ]; then
            echo "❌ One or more required tokens are missing"
            exit 1
          fi

          echo "✅ All required tokens are configured"
        env:
          GH_ACTIONS_TOKEN: ${{ secrets.GH_ACTIONS_TOKEN }}
          NPM_PUBLISH_TOKEN: ${{ secrets.NPM_PUBLISH_TOKEN }}

  lint:
    name: Lint
    needs: test-secret
    runs-on: ubuntu-latest
    if: github.event.pull_request.head.repo.full_name == github.repository
    steps:
      - name: Debug PR info
        run: |
          echo "PR from: ${{ github.event.pull_request.head.repo.full_name }}"
          echo "Base repo: ${{ github.repository }}"
          echo "Is fork: ${{ github.event.pull_request.head.repo.full_name != github.repository }}"
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
      - name: Install dependencies
        run: npm ci --legacy-peer-deps
      - name: Run linter
        run: npm run lint

  check-and-bump-version:
    needs: lint
    runs-on: ubuntu-latest
    if: github.event.pull_request.head.repo.full_name == github.repository
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_ACTIONS_TOKEN }}
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.ref }}
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
      - name: Install dependencies
        run: npm ci --legacy-peer-deps
      - name: Check and bump version if needed
        run: |
          # Get current version from package.json
          CURRENT_VERSION=$(node -p "require('./package.json').version")
          echo "Current version: ${CURRENT_VERSION}"

          # Get latest stable NPM version (excluding alpha/beta)
          LATEST_NPM_VERSION=$(npm view react-jsonschema-form-extras dist-tags.latest 2>/dev/null || echo "0.0.0")
          echo "Latest NPM version: ${LATEST_NPM_VERSION}"

          # Clean current version (remove any prerelease suffix)
          CLEAN_CURRENT_VERSION=$(echo "${CURRENT_VERSION}" | sed 's/-.*$//')
          echo "Clean version: ${CLEAN_CURRENT_VERSION}"

          # Check if version bump is needed using Node.js built-in comparison
          NEEDS_BUMP=$(node -e "
            const current = '${CLEAN_CURRENT_VERSION}'.split('.').map(Number);
            const npm = '${LATEST_NPM_VERSION}'.split('.').map(Number);
            
            // Compare major.minor.patch
            for (let i = 0; i < 3; i++) {
              if (current[i] < npm[i]) {
                console.log('true'); // need bump
                process.exit(0);
              } else if (current[i] > npm[i]) {
                console.log('false'); // no bump
                process.exit(0);
              }
            }
            console.log('true'); // versions are equal, need bump
          ")

          if [ "${NEEDS_BUMP}" = "true" ]; then
            echo "📦 Version bump needed"
            
            # Reset to clean version if it has prerelease suffix
            if [[ "${CURRENT_VERSION}" != "${CLEAN_CURRENT_VERSION}" ]]; then
              npm version "${CLEAN_CURRENT_VERSION}" --no-git-tag-version
            fi
            
            # Bump patch version
            NEW_VERSION=$(npm version patch --no-git-tag-version)
            echo "🚀 Bumped to: ${NEW_VERSION}"
            
            # Commit to PR branch
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add package.json package-lock.json
            git commit -m "🔖 Bump version to ${NEW_VERSION}"
            git push origin ${{ github.event.pull_request.head.ref }}
            
            echo "✅ Version bumped and committed"
          else
            echo "✅ Version is already newer than NPM"
          fi

  deploy-alpha:
    needs: check-and-bump-version
    runs-on: ubuntu-latest
    if: github.event.pull_request.head.repo.full_name == github.repository
    steps:
      - name: Checkout code (after version bump)
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_ACTIONS_TOKEN }}
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.ref }}
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
      - name: Install dependencies
        run: npm ci --legacy-peer-deps
      - name: Build project
        run: npm run dist
      - name: Validate build output
        run: |
          if [ ! -d "lib" ]; then
            echo "❌ lib directory not found"
            exit 1
          fi
          if [ ! -d "dist" ]; then
            echo "❌ dist directory not found"
            exit 1
          fi
          echo "✅ Build output validated (lib and dist folders created)"
      - name: Authenticate with NPM
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_PUBLISH_TOKEN }}
        run: echo "//registry.npmjs.org/:_authToken=${NODE_AUTH_TOKEN}" > ~/.npmrc
      - name: Generate alpha version
        id: generate_version
        run: |
          CURRENT_VERSION=$(node -p "require('./package.json').version")
          TIMESTAMP=$(date +'%H%M%S')
          ALPHA_VERSION="${CURRENT_VERSION}-alpha.${TIMESTAMP}"
          echo "alpha_version=${ALPHA_VERSION}" >> $GITHUB_ENV

          # Update package.json only for publishing (without committing)
          jq ".version = \"${ALPHA_VERSION}\"" package.json > package.json.tmp && mv package.json.tmp package.json
          npm ci --legacy-peer-deps
      - name: Publish Alpha Version
        run: |
          npm publish --tag alpha
          echo "✅ Successfully published alpha version: ${alpha_version}"
          echo ""
          echo "🚀 To test this alpha version, run:"
          echo "npm install react-jsonschema-form-extras@${alpha_version}"
        if: endsWith(github.event.pull_request.user.login, '[bot]') == false
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_PUBLISH_TOKEN }}
          alpha_version: ${{ env.alpha_version }}
